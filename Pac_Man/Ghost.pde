public class Ghost extends Entity {
  color clr;
  final int pointVal = 200;
  
  Ghost(int startX, int startY, color startClr) {
    super(startX, startY);
    clr = startClr;
  }
  
  void move() {
    
  }
}
